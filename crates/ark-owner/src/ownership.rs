// TODO
// if mint add token to owner in db for collection_address && token_id
// if transfer remove token from owner in db for collection_address && token_id
// if transfer add token to owner in db for collection_address && token_id