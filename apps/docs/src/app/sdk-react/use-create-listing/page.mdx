export const metadata = {
  title: 'useCreateListing',
  description: 'Hook for creating listings.',
}

# useCreateListing

Hook for creating listings.

## Import

```typescript
import { useCreateListing } from '@ark-project/react'
```

## Usage

```typescript
import { useAccount } from '@starknet-react/core'
import { useCreateListing } from '@ark-project/react';

function App({ tokenAddress, tokenId, startAmount }) {
  const { account } = useAccount();
  const { createListing, data, isLoading, isSuccess } = useCreateListing();

return (
    <>
      <button onClick={async () => {
        await createListing({
          account,
          tokenAddress,
          tokenId,
          amount: BigInt(startAmount),
        });
      }}>
        Create Listing
      </button>
      <div>Status: {status}</div>
      {response && <div>Response: {response.toString()}</div>}
    </>
  );
};
```

## Return Type

```tsx
import { type useCreateListingReturnType } from '@ark-project/react'
```

<Properties>
  <Property
    name="createListing"
    type="(params: CreateListingParams) => CreateListingResult"
  >
    Mutation to create a listing. See
    [CreateListingParams](/sdk-core/create-listing).
  </Property>
  <Property
    name="createListingAsync"
    type="(params: CreateListingParams) => Promise<CreateListingResult>"
  >
    Async mutation to create a listing. See
    [CreateListingParams](/sdk-core/create-auction).
  </Property>
  <Property name="data" type="CreateListingResult">
    The data returned from the mutation.
  </Property>
</Properties>

## Parameters

<Properties>
  <Property name="config (optional)" type="Config | undefined">
    Config to use instead of retrieving from the nearest ArkProvider.
  </Property>
</Properties>

## Actions

- [createListing](/sdk-core/create-listing)

[CreateListingParameters](/sdk-core/create-listing#parameters) and [CreateListingResponse](/sdk-core/create-listing#response) for more details.
