export const metadata = {
  title: 'useConfig',
  description: 'Hook for getting Config from nearest ArkProvider',
}

# useConfig

Hook for getting [Config](/sdk-core/configuration) from nearest [ArkProvider](/sdk-react/getting-started#react-next-js-application-setup-with-stark-net-provider).

## Import

```typescript
import { useConfig } from '@ark-project/react'
```

## Usage

```tsx
import React from 'react'
import { useConfig } from '@ark-project/react'

function App = () => {
  const config = useConfig()
}
```

## Return Type

`Config | undefined`

Config to use instead of retrieving from the from nearest ArkProvider.
