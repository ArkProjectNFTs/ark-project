export const metadata = {
  title: 'useFulfillListing',
  description: 'Hook for fulfilling listings.',
}

# useFulfillListing

Hook for fulfilling listings.

## Import

```typescript
import { useFulfillListing } from '@ark-project/react'
```

## Usage

```tsx
import { useAccount } from '@starknet-react/core'
import { useFulfillListing } from '@ark-project/react'

function FulfillListing({
  orderHash,
  tokenAddress,
  tokenId,
  brokerId,
  startAmount,
  currencyAddress,
}) {
  const { account } = useAccount()
  const { fulfillListing, status } = useFulfillListing()

  return (
    <>
      <button
        onClick={async () => {
          await fulfillListing({
            starknetAccount: account,
            orderHash,
            tokenAddress,
            tokenId,
            brokerId,
            amount: BigInt(startAmount),
            currencyAddress,
          })
        }}
      >
        Fulfill Listing
      </button>
      <div>Status: {status}</div>
    </>
  )
}
```

## Return Type

```ts
import { type useFulfillListingReturnType } from '@ark-project/react'
```

<Properties>
  <Property
    name="fulfillListing"
    type="(params: FulfillListingParams) => FulfillListingResult"
  >
    Mutation to fulfill a listing. See
    [FulfillListingParams](/sdk-core/fulfill-listing).
  </Property>
  <Property
    name="fulfillListingAsync"
    type="(params: FulfillListingParams) => Promise<FulfillListingResult>"
  >
    Async mutation to fulfill a listing. See
    [FulfillListingParams](/sdk-core/fulfill-listing).
  </Property>
  <Property name="data" type="FulfillListingResult">
    The data returned from the mutation.
  </Property>
</Properties>

## Parameters

<Properties>
  <Property name="config (optional)" type="Config | undefined">
    Config to use instead of retrieving from the nearest ArkProvider.
  </Property>
</Properties>

## Actions

- [fulfillListing](/sdk-core/fulfill-listing)

[FulfillListingParameters](/sdk-core/fulfill-listing#parameters) and [FulfillListingResponse](/sdk-core/fulfill-listing#response) for more details.
